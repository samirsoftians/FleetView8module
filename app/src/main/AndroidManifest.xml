<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.fleetviewandroid"
    android:versionCode="1"
    android:versionName="1.0">

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="18" />

    <permission
        android:name="com.app.maps.permission.MAPS_RECEIVE"
        android:protectionLevel="signature" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/trans"
        android:label="@string/app"
        android:theme="@style/AppTheme">
        <activity
            android:name=".LoginForm"
            android:label="FleetView Application">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".AddRequest"
            android:label="FleetView Application" />
        <activity android:name=".SelectUser" />
        <activity
            android:name=".MainForm"
            android:label="FleetView Application" />
        <activity
            android:name=".OdoMeter"
            android:label="FleetView Application" />
        <activity android:name=".ViewL1Report" />
        <activity
            android:name=".Violation"
            android:label="FleetView Application" />
        <activity android:name=".TodayViolationdetails" />
        <activity android:name=".ViewViloationReport" />
        <activity android:name=".RARDViolationdetails" />
        <activity android:name=".CDViolationdetails" />
        <activity
            android:name=".Alerts"
            android:label="FleetVIew Application" />
        <activity android:name=".L1Report" />
        <activity android:name=".FuelEntry" />
        <!-- <activity android:name=".VehicleCheckableList"/> -->


        <!--
  <meta-data
  android:name="com.google.android.gms.version"
  android:value="@integer/google_play_services_version"/>
        -->

        <activity android:name=".CurrentPosition" />
        <activity android:name=".Vehicle_Details" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyBH8gA0T21FgfYVYBE5Hy3H4k_i6nFE__E" />

        <activity
            android:name=".Safe2SchoolActivity"
            android:label="@string/title_activity_safe2_school" />
        <activity
            android:name=".MapSafe2School"
            android:label="@string/title_activity_map_safe2_school" />
        <activity android:name=".TripEntryForm" />
        <activity android:name=".DeviceStatus"></activity>
    </application>

</manifest>